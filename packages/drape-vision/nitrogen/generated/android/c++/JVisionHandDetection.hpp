///
/// JVisionHandDetection.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "VisionHandDetection.hpp"

#include "JVisionHandedness.hpp"
#include "JVisionLandmark.hpp"
#include "VisionHandedness.hpp"
#include "VisionLandmark.hpp"
#include <optional>
#include <vector>

namespace margelo::nitro::drape::vision {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "VisionHandDetection" and the the Kotlin data class "VisionHandDetection".
   */
  struct JVisionHandDetection final: public jni::JavaClass<JVisionHandDetection> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/drape/vision/VisionHandDetection;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct VisionHandDetection by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    VisionHandDetection toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldLandmarks = clazz->getField<jni::JArrayClass<JVisionLandmark>>("landmarks");
      jni::local_ref<jni::JArrayClass<JVisionLandmark>> landmarks = this->getFieldValue(fieldLandmarks);
      static const auto fieldWorldLandmarks = clazz->getField<jni::JArrayClass<JVisionLandmark>>("worldLandmarks");
      jni::local_ref<jni::JArrayClass<JVisionLandmark>> worldLandmarks = this->getFieldValue(fieldWorldLandmarks);
      static const auto fieldHandedness = clazz->getField<JVisionHandedness>("handedness");
      jni::local_ref<JVisionHandedness> handedness = this->getFieldValue(fieldHandedness);
      static const auto fieldConfidence = clazz->getField<jni::JDouble>("confidence");
      jni::local_ref<jni::JDouble> confidence = this->getFieldValue(fieldConfidence);
      return VisionHandDetection(
        [&](auto&& __input) {
          size_t __size = __input->size();
          std::vector<VisionLandmark> __vector;
          __vector.reserve(__size);
          for (size_t __i = 0; __i < __size; __i++) {
            auto __element = __input->getElement(__i);
            __vector.push_back(__element->toCpp());
          }
          return __vector;
        }(landmarks),
        worldLandmarks != nullptr ? std::make_optional([&](auto&& __input) {
          size_t __size = __input->size();
          std::vector<VisionLandmark> __vector;
          __vector.reserve(__size);
          for (size_t __i = 0; __i < __size; __i++) {
            auto __element = __input->getElement(__i);
            __vector.push_back(__element->toCpp());
          }
          return __vector;
        }(worldLandmarks)) : std::nullopt,
        handedness != nullptr ? std::make_optional(handedness->toCpp()) : std::nullopt,
        confidence != nullptr ? std::make_optional(confidence->value()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JVisionHandDetection::javaobject> fromCpp(const VisionHandDetection& value) {
      using JSignature = JVisionHandDetection(jni::alias_ref<jni::JArrayClass<JVisionLandmark>>, jni::alias_ref<jni::JArrayClass<JVisionLandmark>>, jni::alias_ref<JVisionHandedness>, jni::alias_ref<jni::JDouble>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        [&](auto&& __input) {
          size_t __size = __input.size();
          jni::local_ref<jni::JArrayClass<JVisionLandmark>> __array = jni::JArrayClass<JVisionLandmark>::newArray(__size);
          for (size_t __i = 0; __i < __size; __i++) {
            const auto& __element = __input[__i];
            auto __elementJni = JVisionLandmark::fromCpp(__element);
            __array->setElement(__i, *__elementJni);
          }
          return __array;
        }(value.landmarks),
        value.worldLandmarks.has_value() ? [&](auto&& __input) {
          size_t __size = __input.size();
          jni::local_ref<jni::JArrayClass<JVisionLandmark>> __array = jni::JArrayClass<JVisionLandmark>::newArray(__size);
          for (size_t __i = 0; __i < __size; __i++) {
            const auto& __element = __input[__i];
            auto __elementJni = JVisionLandmark::fromCpp(__element);
            __array->setElement(__i, *__elementJni);
          }
          return __array;
        }(value.worldLandmarks.value()) : nullptr,
        value.handedness.has_value() ? JVisionHandedness::fromCpp(value.handedness.value()) : nullptr,
        value.confidence.has_value() ? jni::JDouble::valueOf(value.confidence.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::drape::vision
