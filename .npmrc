auto-install-peers=true
shamefully-hoist=false
strict-peer-dependencies=false

# Allow build scripts for native modules
approve-builds=true

# Hoist metro packages so Expo CLI can resolve them
public-hoist-pattern[]=metro*
