diff --git a/lib/hooks/internal/useCameraSession.d.ts b/lib/hooks/internal/useCameraSession.d.ts
index eb6b7ed8aebe86c090884c0d2cce21af4e36d921..7ce809cc4862c635dbe82df40670282c0dec3888 100644
--- a/lib/hooks/internal/useCameraSession.d.ts
+++ b/lib/hooks/internal/useCameraSession.d.ts
@@ -1,6 +1,8 @@
+import type { CameraOutput } from '../../specs/outputs/CameraOutput.nitro';
 import type { CameraSession } from '../../specs/session/CameraSession.nitro';
 interface Props {
     enableMultiCamSupport: boolean;
+    outputs: CameraOutput[];
 }
-export declare function useCameraSession({ enableMultiCamSupport, }: Props): CameraSession | undefined;
+export declare function useCameraSession({ enableMultiCamSupport, outputs, }: Props): CameraSession | undefined;
 export {};
diff --git a/lib/hooks/internal/useCameraSession.js b/lib/hooks/internal/useCameraSession.js
index 8473c19d924081b846ea8b4b1d4aee3b17f7c885..13d74d5d88da87907abc22421d19a400026c566c 100644
--- a/lib/hooks/internal/useCameraSession.js
+++ b/lib/hooks/internal/useCameraSession.js
@@ -1,7 +1,9 @@
-import { useEffect, useState } from 'react';
+import { useEffect, useRef, useState } from 'react';
 import { VisionCamera } from '../../VisionCamera';
-export function useCameraSession({ enableMultiCamSupport, }) {
+export function useCameraSession({ enableMultiCamSupport, outputs, }) {
     const [session, setSession] = useState();
+    const outputsRef = useRef(outputs);
+    outputsRef.current = outputs;
     // session creation
     useEffect(() => {
         let isCanceled = false;
@@ -21,9 +23,25 @@ export function useCameraSession({ enableMultiCamSupport, }) {
     // session teardown
     useEffect(() => {
         return () => {
-            // remove all connections
-            session?.stop();
-            session?.configure([]);
+            if (session == null)
+                return;
+            // Keep the session and all outputs alive until AVFoundation has stopped
+            // and detached the capture graph. Hermes can otherwise collect the
+            // preview output while configure([]) is still queued.
+            const retainedSession = session;
+            const retainedOutputs = outputsRef.current;
+            void (async () => {
+                try {
+                    await retainedSession.stop();
+                    await retainedSession.configure([]);
+                }
+                catch (error) {
+                    console.warn('Failed to tear down CameraSession safely:', error);
+                }
+                finally {
+                    void retainedOutputs.length;
+                }
+            })();
         };
     }, [session]);
     return session;
diff --git a/lib/hooks/useCamera.js b/lib/hooks/useCamera.js
index 20f33790dbefecee49047d99a62a6f98906be109..e0da3cfa70e4e967413111329a29aace0ffa2679 100644
--- a/lib/hooks/useCamera.js
+++ b/lib/hooks/useCamera.js
@@ -29,7 +29,10 @@ function defaultOnErrorHandler(error) {
  */
 export function useCamera({ isActive, device, outputs = [], constraints, onSessionConfigSelected, mirrorMode, onConfigured, orientationSource = 'device', onStarted, onStopped, onError = defaultOnErrorHandler, onInterruptionStarted, onInterruptionEnded, onSubjectAreaChanged, enableDistortionCorrection, enableLowLightBoost, enableSmoothAutoFocus, getInitialExposureBias, getInitialZoom, }) {
     // 1. Create session
-    const session = useCameraSession({ enableMultiCamSupport: false });
+    const session = useCameraSession({
+        enableMultiCamSupport: false,
+        outputs,
+    });
     // 2. Update output orientations
     const orientationSourceOrUndefined = orientationSource === 'custom' ? undefined : orientationSource;
     const orientation = useOrientation(orientationSourceOrUndefined);
diff --git a/nitrogen/generated/shared/c++/views/HybridFrameRendererViewComponent.cpp b/nitrogen/generated/shared/c++/views/HybridFrameRendererViewComponent.cpp
index fc6741057b7ef3c2231ca700c121bcb895febe89..082f8e307eb61b67b34cd0fe695c7d96f8191120 100644
--- a/nitrogen/generated/shared/c++/views/HybridFrameRendererViewComponent.cpp
+++ b/nitrogen/generated/shared/c++/views/HybridFrameRendererViewComponent.cpp
@@ -57,7 +57,7 @@ namespace margelo::nitro::camera::views {

   HybridFrameRendererViewComponentDescriptor::HybridFrameRendererViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
     : ConcreteComponentDescriptor(parameters,
-                                  react::RawPropsParser()) {}
+                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

   std::shared_ptr<const react::Props> HybridFrameRendererViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                              const std::shared_ptr<const react::Props>& props,
diff --git a/nitrogen/generated/shared/c++/views/HybridPreviewViewComponent.cpp b/nitrogen/generated/shared/c++/views/HybridPreviewViewComponent.cpp
index d9eac55a283fdf587c86662bc04e1bd9660502c5..f77e785eb8320f5752dfe7ae63b4751732151538 100644
--- a/nitrogen/generated/shared/c++/views/HybridPreviewViewComponent.cpp
+++ b/nitrogen/generated/shared/c++/views/HybridPreviewViewComponent.cpp
@@ -112,7 +112,7 @@ namespace margelo::nitro::camera::views {

   HybridPreviewViewComponentDescriptor::HybridPreviewViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
     : ConcreteComponentDescriptor(parameters,
-                                  react::RawPropsParser()) {}
+                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

   std::shared_ptr<const react::Props> HybridPreviewViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                        const std::shared_ptr<const react::Props>& props,
diff --git a/src/hooks/internal/useCameraSession.ts b/src/hooks/internal/useCameraSession.ts
index f6d2a1d023e4d82d303a70ecc9074b59deba73cb..aa18b96e0326cfa32caa49466d87a2ac098ecd05 100644
--- a/src/hooks/internal/useCameraSession.ts
+++ b/src/hooks/internal/useCameraSession.ts
@@ -1,15 +1,20 @@
-import { useEffect, useState } from 'react'
+import { useEffect, useRef, useState } from 'react'
+import type { CameraOutput } from '../../specs/outputs/CameraOutput.nitro'
 import type { CameraSession } from '../../specs/session/CameraSession.nitro'
 import { VisionCamera } from '../../VisionCamera'

 interface Props {
   enableMultiCamSupport: boolean
+  outputs: CameraOutput[]
 }

 export function useCameraSession({
   enableMultiCamSupport,
+  outputs,
 }: Props): CameraSession | undefined {
   const [session, setSession] = useState<CameraSession>()
+  const outputsRef = useRef(outputs)
+  outputsRef.current = outputs

   // session creation
   useEffect(() => {
@@ -31,9 +36,26 @@ export function useCameraSession({
   // session teardown
   useEffect(() => {
     return () => {
-      // remove all connections
-      session?.stop()
-      session?.configure([])
+      if (session == null) return
+
+      // Keep the session and all outputs alive until AVFoundation has stopped
+      // and detached the capture graph. Hermes can otherwise collect the
+      // preview output while configure([]) is still queued, which releases its
+      // AVCaptureVideoPreviewLayer against a live capture-session connection.
+      const retainedSession = session
+      const retainedOutputs = outputsRef.current
+      void (async () => {
+        try {
+          await retainedSession.stop()
+          await retainedSession.configure([])
+        } catch (error) {
+          console.warn('Failed to tear down CameraSession safely:', error)
+        } finally {
+          // Referencing the array here deliberately retains its hybrid objects
+          // through both awaited native operations.
+          void retainedOutputs.length
+        }
+      })()
     }
   }, [session])

diff --git a/src/hooks/useCamera.ts b/src/hooks/useCamera.ts
index 1abf0fbf5fc384526a177b327527318d990fa2b4..cc255eec1b1b03f0fd6909b9025dc29f7158a23c 100644
--- a/src/hooks/useCamera.ts
+++ b/src/hooks/useCamera.ts
@@ -241,7 +241,10 @@ export function useCamera({
   getInitialZoom,
 }: CameraProps): CameraController | undefined {
   // 1. Create session
-  const session = useCameraSession({ enableMultiCamSupport: false })
+  const session = useCameraSession({
+    enableMultiCamSupport: false,
+    outputs,
+  })

   // 2. Update output orientations
   const orientationSourceOrUndefined =
