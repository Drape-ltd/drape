/**
 * Drape — E2E seed script entry point
 *
 * Delegates to packages/db/seed-e2e.ts which has access to Prisma + supabase-js.
 *
 * Usage (from monorepo root):
 *   pnpm --filter @drape/db exec tsx seed-e2e.ts
 *
 * Or directly:
 *   cd packages/db && npx tsx seed-e2e.ts
 */

// Re-export for documentation purposes — actual script is packages/db/seed-e2e.ts
export {}
